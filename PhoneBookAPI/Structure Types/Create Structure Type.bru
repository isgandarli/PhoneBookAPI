meta {
  name: Create Structure Type
  type: http
  seq: 3
}

post {
  url: {{base_url}}/structure-types
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "name": "İdarə"
  }
}

settings {
  encodeUrl: true
}

docs {
  Create a new structure type. Name must be unique.
}
